syntax = "proto3";

package catalog.v1;

service CatalogService {
  rpc GetInventory (GetInventoryRequest) returns (GetInventoryResponse);
}

message GetInventoryRequest {
  string sku = 1;
}

message GetInventoryResponse {
  string sku = 1;
  int32 quantity_on_hand = 2;
  bool backorderable = 3;
}
